# Gitleaks configuration for AudiencePulse
# https://github.com/gitleaks/gitleaks

title = "AudiencePulse Gitleaks Config"

[allowlist]
description = "Global allowlist"
paths = [
  '''\.env\.example''',
  '''README\.md''',
  '''requirements\.txt''',
]
